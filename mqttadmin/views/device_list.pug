extends layout

block content
    h1= title

    ul
    each device in device_list
        li
            a(href=device.url) #{device.name}
            |  (#{device.type})

    else
        li There are no devices.