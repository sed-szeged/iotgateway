extends layout

block content
  h1= title
  p Welcome to #[em MQTT Admin]

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The MQTT Admin has the following record counts:

    ul
      li #[strong Devices:] !{data.device_count}
